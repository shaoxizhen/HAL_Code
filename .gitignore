# ============================================================
#  E:\STM32_Code\Test —— STM32 基础实验工程仓库
#  原则：只提交"自己写的代码 + 工程配置"，不提交库副本和编译产物
# ============================================================

# ---------- 1. CMSIS / HAL 驱动副本 ----------
# 每个 CubeMX 工程各复制一份，约 65 MB/工程，7 个工程内容完全相同
Drivers/

# ---------- 2. Keil MDK 编译输出 ----------
# CubeMX 生成的 Keil 工程把编译结果放在 MDK-ARM/<工程名>/ 下，名字与工程同名
# 注意：带 "/" 的规则默认锚定在仓库根目录，这里 MDK-ARM 在各工程目录下，
# 所以必须写成 **/ 开头，否则规则不生效
**/MDK-ARM/*/
!**/MDK-ARM/RTE/
!**/MDK-ARM/RTE/**
**/MDK-ARM/Objects/
**/MDK-ARM/Listings/
**/MDK-ARM/DebugConfig/
*.uvguix.*
*.o
*.d
*.crf
*.dep
*.lnp
*.sct
*.axf
*.hex
*.bin
*.map
*.iex
*.htm
*.lst
*.scvd

# ---------- 3. 临时文件 / 个人文件 ----------
_encoding_backup/
*.docx
*.bak
*.orig
*~

# ============================================================
# 以下内容必须保留在仓库里，不要加进忽略列表：
#   <工程名>.ioc                CubeMX 配置，重新生成工程的依据
#   .mxproject                  CubeMX 生成的工程元数据
#   MDK-ARM/<工程名>.uvprojx    Keil 工程文件
#   MDK-ARM/<工程名>.uvoptx     调试器 / 烧录算法设置
#   MDK-ARM/RTE/                Keil 组件配置（上面的 ! 规则专门放行）
#   Core/Inc  Core/Src          自己写的代码
# ============================================================
